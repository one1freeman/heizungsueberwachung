services:
  homeassistant:
    build: ./homeassistant
    container_name: homeassistant
    ports:
      - "8123:8123"
    restart: unless-stopped
  mqtt:
    build: ./mosquitto
    container_name: mqtt
    ports:
      - "1883:1883"
      - "9001:9001"
    restart: unless-stopped